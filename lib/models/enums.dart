/// Enum para estágio de desenvolvimento da startup
enum EstagioDesenvolvimento { nova, emOperacao, emExpansao }

/// Enum para tipo de oferta
enum TipoOferta { compra, venda }

/// Enum para status da oferta
enum StatusOferta { aberta, concluida, cancelada }
